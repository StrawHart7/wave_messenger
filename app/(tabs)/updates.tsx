import { PhaseStub, Screen } from '../../components/ui/Screen';

export default function UpdatesScreen() {
  return (
    <Screen title="Updates">
      <PhaseStub phase={6} what="Status and channels" />
    </Screen>
  );
}
