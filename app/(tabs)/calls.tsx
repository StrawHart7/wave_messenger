import { PhaseStub, Screen } from '../../components/ui/Screen';

export default function CallsScreen() {
  return (
    <Screen title="Calls">
      <PhaseStub phase={7} what="Call history" />
    </Screen>
  );
}
